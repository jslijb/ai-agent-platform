- [x] api_keys.yaml 中 tushare 和 tickflow 已提升为顶层节点
- [x] api_keys.yaml 中 models 列表的 context 字段值与模型实际上下文窗口一致且格式统一（大写K/M后缀）
- [x] api_keys.yaml 中 models 列表的 description 描述准确反映模型实际能力定位
- [x] Python 端 _resolve_env_values 只对全大写+下划线格式字符串做环境变量替换，其他保留原值
- [x] TypeScript 端 resolveEnvVars 采用与 Python 端一致的智能解析策略
- [x] Python 端新增 get_raw_config / get_raw_value 函数
- [x] bailian.ts 通过 config 模块读取 DASHSCOPE_API_KEY 和 BAILIAN_MODEL
- [x] get_value("tushare", "TUSHARE_TOKEN") 可正确返回配置值
- [x] get_value("tickflow", "TICKFLOW_API_KEY") 可正确返回配置值
- [x] models 列表的 id/name/description/context 等字面量字符串不再被错误解析为 None/空串
- [x] 前端模型选择下拉框中 context 值显示正确（如 "256K上下文"、"262K上下文"、"1M上下文"）
