# 微服务架构升级 SDD + TDD 测试报告

**自动生成时间**: 2026/6/3 16:40:11
**测试脚本**: scripts/test-microservice-integration.ts
**测试环境**: Docker Compose（10 容器，Windows 本地部署）
**测试方法**: SDD（契约驱动开发）+ TDD（测试驱动开发）

---

## 1. 测试概览

| 指标 | 数值 |
|---|---|
| 总测试用例 | 53 |
| 通过 | 52 |
| 失败 | 1 |
| 通过率 | **98.1%** |
| Spec 覆盖率 | 13/13 Requirement 全覆盖 |

---

## 2. 逐项测试结果

| # | 测试用例 | 结果 | 详情 |
|---|---|---|---|
| R1.1 RAG 服务独立运行在端口 3001 | ✅ pass |  |
| R1.2 LLM Gateway 独立运行在端口 3002（dev 环境） | ✅ pass |  |
| R1.3 主服务独立运行在端口 3000 | ✅ pass |  |
| R1.4 Docker 容器全部 healthy | ✅ pass |  |
| R1.5 评估服务已合并到 RAG 服务 | ✅ pass |  |
| R2.1 主服务→RAG 服务 HTTP REST 调用 | ✅ pass |  |
| R2.2 主服务→LLM Gateway HTTP REST 调用 | ❌ fail | timeout |
| R2.3 评估任务通过 BullMQ 异步执行 | ✅ pass |  |
| R2.4 评估任务状态可查询 | ✅ pass |  |
| R3.1 GET /api/health → { status, uptime, service, details } | ✅ pass |  |
| R3.2 POST /api/retrieve → { success, results, latencyMs } | ✅ pass |  |
| R3.3 POST /api/retrieve 缺少 query → 400 | ✅ pass |  |
| R3.4 POST /api/embed → { embeddings } | ✅ pass |  |
| R3.5 POST /api/rerank → { results } | ✅ pass |  |
| R3.6 POST /api/chunk → { chunks } | ✅ pass |  |
| R4.1 GET /api/health → { status, uptime, service, details } | ✅ pass |  |
| R4.2 POST /api/llm/chat → { content, model, usage } | ✅ pass |  |
| R4.3 POST /api/llm/chat 缺少 messages → 400 | ✅ pass |  |
| R4.4 GET /api/llm/usage → { totalTokens, byModel, byDate } | ✅ pass |  |
| R5.1 POST /api/evaluation/run → { taskId, status: "queued" } | ✅ pass |  |
| R5.2 GET /api/evaluation/status/:taskId → { taskId, status, progress? } | ✅ pass |  |
| R5.3 GET /api/evaluation/results → { versions } | ✅ pass |  |
| R6.1 RAG 检索接口一致性：跨服务调用返回与进程内相同结构 | ✅ pass |  |
| R6.2 超时控制：RAG 服务 30 秒内响应 | ✅ pass |  |
| R6.3 traceId 透传：SDK 自动注入 X-Trace-Id Header | ✅ pass |  |
| R7.1 Nginx / → 主服务前端 SSR（接受 2xx/3xx） | ✅ pass |  |
| R7.2 Nginx /api/health → 主服务 health | ✅ pass |  |
| R7.3 Nginx /api/evaluation/ → RAG 服务 | ✅ pass |  |
| R7.4 Nginx /api/data/ → Data Service | ✅ pass |  |
| R7.5 Nginx /internal/ → 403 禁止外部访问 | ✅ pass |  |
| R7.6 Nginx /nginx-health → 200 | ✅ pass |  |
| R7.7 Nginx /api/auth/ → 主服务认证 | ✅ pass |  |
| R8.1 RAG 服务接受 X-Trace-Id 并在响应中返回 | ✅ pass |  |
| R8.2 RAG 服务自动生成 traceId（未传入时） | ✅ pass |  |
| R8.3 LLM Gateway 接受并返回 X-Trace-Id | ✅ pass |  |
| R8.4 Evaluation 端点也返回 X-Trace-Id | ✅ pass |  |
| R9.1 RAG 服务和 Main 服务连接同一 PostgreSQL | ✅ pass |  |
| R9.2 RAG 服务 Redis 连接正常 | ✅ pass |  |
| R10.1 USE_MICROSERVICE 环境变量已设置为 true | ✅ pass |  |
| R10.2 微服务模式下 service-adapter 通过 HTTP 调用（非进程内） | ✅ pass |  |
| R10.3 RAG_SERVICE_URL 环境变量指向 rag-service | ✅ pass |  |
| R10.4 服务失败时正确抛出错误（不做进程内降级） | ✅ pass |  |
| R11.1 RAG /api/retrieve 延迟 < 5000ms（含模型推理） | ✅ pass |  |
| R11.2 RAG /api/health 延迟 < 100ms（轻量端点） | ✅ pass |  |
| R11.3 Nginx 代理额外延迟 < 50ms | ✅ pass |  |
| R11.4 LLM Gateway /api/health 延迟 < 100ms | ✅ pass |  |
| R12.1 Main Service /api/health 在所有服务运行时正常 | ✅ pass |  |
| R12.2 Data Service /health 正常 | ✅ pass |  |
| R12.3 Docker restart policy 配置正确 | ✅ pass |  |
| R13.1 所有生产服务容器运行中 | ✅ pass |  |
| R13.2 LLM Gateway 仅在 dev profile 运行 | ✅ pass |  |
| R13.3 服务间通过 Docker DNS 互相访问 | ✅ pass |  |
| R13.4 数据卷持久化配置 | ✅ pass |  |

---

## 3. Spec 覆盖率

| Spec Requirement | 通过/总数 | 覆盖率 |
|---|---|---|
| R1 微服务拆分架构 | 5/5 | ✅ 100% |
| R2 服务间通信协议 | 3/4 | ❌ 75% |
| R3 RAG 服务 REST API | 6/6 | ✅ 100% |
| R4 LLM Gateway REST API | 4/4 | ✅ 100% |
| R5 评估服务异步执行 | 3/3 | ✅ 100% |
| R6 Client SDK 薄封装 | 3/3 | ✅ 100% |
| R7 API Gateway 路由 | 7/7 | ✅ 100% |
| R8 分布式追踪 traceId | 4/4 | ✅ 100% |
| R9 数据库共享策略 | 2/2 | ✅ 100% |
| R10 降级开关 | 4/4 | ✅ 100% |
| R11 性能基线 | 4/4 | ✅ 100% |
| R12 故障隔离 | 3/3 | ✅ 100% |
| R13 Docker Compose 编排 | 4/4 | ✅ 100% |

---

## 4. 失败详情

1. R2.2 主服务→LLM Gateway HTTP REST 调用: timeout

---

*本报告由测试脚本自动生成，非人工编写*
